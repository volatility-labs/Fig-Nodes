
import BaseCustomNode from '../base/BaseCustomNode';

export default class ATRIndicatorNodeUI extends BaseCustomNode {
    constructor(title: string, data: any) {
        super(title, data);
    }
}
